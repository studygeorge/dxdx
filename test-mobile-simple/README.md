# 🧪 ТЕСТОВЫЙ СЕРВЕР ДЛЯ ОТЛАДКИ МОБИЛЬНОЙ ВЕРСИИ

## 🎯 Цель:
Запустить **МИНИМАЛЬНЫЙ** HTML-сервер чтобы проверить:
- Работает ли сервер вообще на мобильном
- Проблема в Next.js или в чём-то другом
- Загружается ли простой HTML на телефоне

---

## 📦 Что внутри:
- `index.html` - минимальная HTML страница (только текст, никакого JS фреймворка)
- `server.js` - простой Express сервер на порту 3001
- `nginx-test.conf` - конфиг Nginx для тестового поддомена

---

## 🚀 УСТАНОВКА НА СЕРВЕРЕ:

### Шаг 1: Получить код

```bash
cd /home/dxdx-repo
git pull origin main
```

### Шаг 2: Установить зависимости

```bash
cd test-mobile-simple
npm install
```

### Шаг 3: Запустить тестовый сервер

**Вариант A: Напрямую (для быстрого теста)**
```bash
node server.js
```

**Вариант B: Через PM2 (рекомендую)**
```bash
pm2 start server.js --name test-mobile
pm2 save
```

### Шаг 4: Настроить Nginx (опционально)

**Если хочешь использовать поддомен `test.dxcapital-ai.com`:**

```bash
# 1. Добавить DNS запись (у регистратора домена)
# test.dxcapital-ai.com -> IP сервера

# 2. Скопировать конфиг
sudo cp /home/dxdx-repo/test-mobile-simple/nginx-test.conf /etc/nginx/sites-available/test.dxcapital-ai.com

# 3. Создать симлинк
sudo ln -s /etc/nginx/sites-available/test.dxcapital-ai.com /etc/nginx/sites-enabled/

# 4. Проверить и перезагрузить
sudo nginx -t && sudo systemctl reload nginx
```

**Если не хочешь поддомен, просто используй порт:**
```
http://IP_СЕРВЕРА:3001
```

---

## 📱 ТЕСТИРОВАНИЕ:

### Открой на телефоне:

**Вариант 1: Через IP и порт (работает сразу)**
```
http://ВАШ_IP:3001
```

**Вариант 2: Через поддомен (если настроил DNS)**
```
https://test.dxcapital-ai.com
```

### Что увидишь:
- ✅ Зелёный экран с информацией об устройстве
- ✅ Ширина/высота экрана
- ✅ User Agent
- ✅ Есть ли тач-скрин
- ✅ Кнопка "Тест API"
- ✅ Кнопка "Тест скролла"
- ✅ Лог загрузки внизу

### Проверь:
1. **Загружается ли страница вообще?**
   - Да → проблема в Next.js/React коде
   - Нет → проблема в сервере/Nginx

2. **Работает ли скролл?**
   - Нажми "Тест скролла" → должен появиться длинный блок
   - Прокрути вниз → должно быть сообщение "Скролл работает!"

3. **Работает ли API?**
   - Нажми "Тест API" → должен быть зелёный ответ от backend

---

## 🔍 ЧТО ЭТО ПОКАЖЕТ:

| Результат | Что это значит |
|-----------|----------------|
| ✅ Тест загружается нормально | Проблема в **Next.js коде** (видео, React, etc.) |
| ❌ Тест НЕ загружается | Проблема в **сервере/Nginx/сети** |
| ✅ Скролл работает в тесте | Проблема в **CSS фронтенда** |
| ❌ Скролл НЕ работает в тесте | Проблема в **браузере/устройстве** |

---

## 📊 ЛОГИ:

### Логи тестового сервера:
```bash
# Если запустил напрямую - логи в терминале

# Если через PM2:
pm2 logs test-mobile
```

### Логи Nginx (если используешь поддомен):
```bash
sudo tail -f /var/log/nginx/test-access.log
sudo tail -f /var/log/nginx/test-error.log
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ:

### Если тест РАБОТАЕТ на мобильном:
✅ Значит проблема в **Next.js коде**
- Ищем тяжёлые компоненты
- Убираем видео
- Упрощаем главную страницу

### Если тест НЕ РАБОТАЕТ на мобильном:
❌ Проблема в **инфраструктуре**
- Проверяем Nginx
- Проверяем файрвол
- Проверяем сеть

---

## 🛑 ОСТАНОВКА:

```bash
# Если запустил напрямую
Ctrl+C

# Если через PM2
pm2 stop test-mobile
pm2 delete test-mobile
```

---

## 📝 БЫСТРЫЙ СТАРТ (1 команда):

```bash
cd /home/dxdx-repo && \
git pull origin main && \
cd test-mobile-simple && \
npm install && \
pm2 start server.js --name test-mobile && \
pm2 save && \
echo "✅ Тестовый сервер запущен на порту 3001"
```

**Потом открой на телефоне:** `http://IP_СЕРВЕРА:3001`

---

## 🚀 ГОТОВО!

Запусти тестовый сервер и проверь на телефоне. Это покажет **в чём именно проблема**.
