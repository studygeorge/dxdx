# 🚀 ОТКАТ + ОПТИМИЗАЦИЯ

## ✅ Что мы узнали:
- Тест загрузился → **Next.js работает!**
- С VPN работает → **проблема в размере видео файлов!**
- `Homepagemobile.mp4` = **14MB** 😱
- `gorizlkloading.mp4` = **11MB** 😱

## 🎯 РЕШЕНИЕ:

### Вариант 1: Откат к оригиналу (с видео как было)
```bash
cd /home/dxdx-repo && \
git pull origin main && \
cd frontend/src/app && \
git show dccab87:frontend/src/app/page.js > page.js && \
cd /home/dxdx-repo/frontend && \
npm run build && \
pm2 restart dxcapai-frontend
```

### Вариант 2: ОПТИМИЗИРОВАННАЯ версия (рекомендую)

Сделаем так:
- **На мобильном:** loading video **НЕ автоплей**, только по клику
- **Фоновое видео:** preload="metadata" (не грузим сразу)
- **Добавим poster** (превью картинку)
- **Прогрессивная загрузка**

Создам оптимизированную версию →

---

## 📊 Размеры видео (проблема):

| Файл | Размер | Проблема |
|------|--------|----------|
| Homepagemobile.mp4 | **14MB** | 😱 Слишком тяжёлое для мобильного |
| gorizlkloading.mp4 | **11MB** | 😱 Слишком тяжёлое для мобильного |
| Homepagepk.mp4 | 9.5MB | ⚠️ Тяжёлое |
| veryicalloading.mp4 | 2.0MB | ✅ Нормально |

---

## 💡 РЕКОМЕНДАЦИЯ:

### Вариант A: Быстрый откат (как было)
```bash
cd /home/dxdx-repo/frontend/src/app && \
mv page.js page-TEST-backup.js && \
git show dccab87:frontend/src/app/page.js > page.js && \
cd /home/dxdx-repo/frontend && \
npm run build && \
pm2 restart dxcapai-frontend
```

### Вариант B: С оптимизацией (нужно 10 мин)
Я создам версию где:
- Loading video грузится **только по клику**
- Фоновое видео **не автоплей на мобильном**
- Используем **poster** картинки

---

## ❓ ЧТО ВЫБИРАЕШЬ?

1. **Откатить как было** → работает через VPN
2. **Оптимизировать** → будет работать даже без VPN

**Скажи что делать и я сделаю!** 🚀
