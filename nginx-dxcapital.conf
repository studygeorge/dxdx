# ═══════════════════════════════════════════════════════════════
# 🚀 DXCAPITAL-AI.COM - Nginx Configuration
# Обновлено: 2025-02-06
# Структура: /home/dxdx-repo/frontend + /home/dxdx-repo/backend
# ═══════════════════════════════════════════════════════════════

# ✅ Редирект www -> non-www
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    
    server_name www.dxcapital-ai.com;
    
    ssl_certificate /etc/letsencrypt/live/dxcapital-ai.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dxcapital-ai.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    
    return 301 https://dxcapital-ai.com$request_uri;
}

# ✅ Основной сервер
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    
    server_name dxcapital-ai.com;
    
    ssl_certificate /etc/letsencrypt/live/dxcapital-ai.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dxcapital-ai.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    
    # ✅ Логи
    access_log /var/log/nginx/dxcapital-access.log;
    error_log /var/log/nginx/dxcapital-error.log;
    
    # ✅ Лимиты для загрузки файлов (KYC документы, видео)
    client_max_body_size 100M;
    client_body_buffer_size 10M;
    client_body_timeout 300s;
    client_header_timeout 300s;
    
    # ✅ Таймауты
    proxy_connect_timeout 300s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
    send_timeout 300s;
    
    # ✅ Буферы
    proxy_buffer_size 128k;
    proxy_buffers 4 256k;
    proxy_busy_buffers_size 256k;

    # ═══════════════════════════════════════════════════════════════
    # 🔌 API Backend (Fastify на порту 4000)
    # Путь на сервере: /home/dxdx-repo/backend
    # ═══════════════════════════════════════════════════════════════
    location /api/ {
        # CORS preflight для OPTIONS запросов
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://dxcapital-ai.com' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS, PATCH' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, X-Requested-With, Cookie' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Access-Control-Max-Age' 86400 always;
            add_header 'Content-Length' 0;
            add_header 'Content-Type' 'text/plain charset=UTF-8';
            return 204;
        }

        # Проксирование на Backend
        proxy_pass http://localhost:4000/api/;
        proxy_http_version 1.1;
        
        # Заголовки
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты для API
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # CORS заголовки для обычных запросов
        add_header 'Access-Control-Allow-Origin' 'https://dxcapital-ai.com' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
    }

    # ═══════════════════════════════════════════════════════════════
    # 📁 Uploads (Backend статика)
    # Путь: /home/dxdx-repo/backend/uploads
    # ═══════════════════════════════════════════════════════════════
    location /uploads/ {
        alias /home/dxdx-repo/backend/uploads/;
        
        # Безопасность
        autoindex off;
        
        # Кэширование для загруженных файлов
        expires 30d;
        add_header Cache-Control "public, immutable";
        
        # Типы файлов
        types {
            image/jpeg jpg jpeg;
            image/png png;
            image/gif gif;
            application/pdf pdf;
        }
    }

    # ═══════════════════════════════════════════════════════════════
    # 🎨 Next.js Frontend (порт 3000 - production режим)
    # Путь на сервере: /home/dxdx-repo/frontend
    # ═══════════════════════════════════════════════════════════════
    
    # ✅ Next.js статика (_next/)
    location /_next/static/ {
        proxy_pass http://localhost:3000/_next/static/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        
        # Агрессивное кэширование для статики
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # ✅ Favicon и статические файлы
    location ~* ^/(favicon\.ico|favicon/|robots\.txt|sitemap\.xml)$ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        
        expires 7d;
        add_header Cache-Control "public";
    }
    
    # ✅ Видео и медиа файлы
    location ~* ^/profile/.*\.(mp4|webm|ogg|mov)$ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Range $http_range;
        proxy_set_header If-Range $http_if_range;
        
        # Поддержка range requests для видео
        proxy_cache_bypass $http_range;
        
        # Кэширование видео
        expires 30d;
        add_header Cache-Control "public, immutable";
        
        # Буферизация для видео
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }
    
    # ✅ JavaScript и JSON - БЕЗ кэша
    location ~* \.(js|jsx|json)$ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Origin $http_origin;
        proxy_set_header Referer $http_referer;
        
        proxy_cache_bypass $http_upgrade;
        
        # Отключаем кэш для JS/JSON
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
        
        expires -1;
    }
    
    # ✅ Все остальные запросы -> Next.js
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # Заголовки для Next.js
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        
        # Передаём оригинальные заголовки (для Server Actions)
        proxy_set_header Origin $http_origin;
        proxy_set_header Referer $http_referer;
        
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }
    
    # ✅ Health check endpoint
    location /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }
}

# ✅ HTTP to HTTPS редирект
server {
    listen 80;
    listen [::]:80;
    
    server_name dxcapital-ai.com www.dxcapital-ai.com;
    
    # ✅ Certbot для SSL
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    
    # Всё остальное -> HTTPS
    location / {
        return 301 https://dxcapital-ai.com$request_uri;
    }
}
